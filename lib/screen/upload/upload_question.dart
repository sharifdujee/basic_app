import 'package:flutter/material.dart';

class UploadQuestion extends StatelessWidget {
  const UploadQuestion({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Upload Question'),
    );
  }
}
