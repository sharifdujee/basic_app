import 'package:flutter/material.dart';

class UploadExam extends StatelessWidget {
  const UploadExam({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Upload Exam'),
    );
  }
}
